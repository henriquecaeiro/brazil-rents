/**
 * An object containing the application's route paths.
 * Using constants for routes makes it easier to manage and update paths.
 * @type {{HOME: string, ABOUT: string, CONTACT: string}}
 */
export const ROUTES = {
  HOME: '/',
  ABOUT: '/sobre',
  CONTACT: '/contato',
};